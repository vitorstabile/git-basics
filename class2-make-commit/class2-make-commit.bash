## the project have to be pushed in github or in master

git status

git add .

git status

git commit -m "another commit"

git push

## if your origin is in (main) not master, use the command below

git push origin main